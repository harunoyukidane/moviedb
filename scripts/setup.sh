#!/usr/bin/env bash
# Stub — completed in phase 7 (TMDB importer + setup).
# On Windows, run under WSL or Git Bash (Docker-only workflow).
set -euo pipefail
echo "setup.sh is a stub; implemented in phase 7."
echo "Phase 1 usage: 'docker compose up --wait' to start services and databases."
